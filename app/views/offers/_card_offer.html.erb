<div class="card-group">
  <div class="card h-100 card-style" style="width: 18rem;">
    <%= link_to offer_path(offer) do %>
      <div class="card-body">
        <div class="card-badge card-badge-<%= offer.category.downcase %>">
          <span><%= offer.category %></span>
        </div>
        <p class="card-district district-style"><%= offer.district %></p>
        <p class="card-title"><strong><%= offer.title %></strong></p>
        <p class="card-frequency district-style"><%= offer.frequency %></p>
        <img src="<%= offer.organization.logo_url %>" class="card-offer-logo avatar-bordered" />
      </div>
    <% end %>
  </div>
</div>
