<div>
  <nav class="navbar navbar-dark navbar-expand-lg navbar-container">
    <div class="d-flex justify-content-between navbar_links_container container">
      <div class="d-flex align-items-center">
        <%= link_to "WOLLENTIER", root_path, class: "navbar-brand" %>
        <%= link_to "Jobs", offers_path, method: :get, class: "main-nav-link nav-spacing" %>
        <% if current_user && current_user.organization? %>
          <%= link_to "Volunteers", users_path, method: :get, class: "main-nav-link" %>
        <% else %>
          <%= link_to "Organizations", organizations_path, method: :get, class: "main-nav-link" %>
        <% end %>
      </div>
        <%# <form class="d-flex" role="links"> %>
            <ul class="navbar-nav d-flex align-items-center">
                <% if user_signed_in? %>
                  <li class="nav item dropdown" style="position: initial !important;">
                    <img class="avatar dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="<%= current_user.profile_url %>">
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                      <li><%= link_to "Profile", edit_user_registration_path, method: :get, class: "dropdown-item" %></li>
                      <li><%= link_to "Dashboard", dashboard_path, method: :get, class: "dropdown-item" %></li>
                      <li><%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %></li>
                    </ul>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to "Sign Up", new_user_registration_path, class: "nav-btn-primary" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Login", new_user_session_path, class: "main-nav-link" %>
                  </li>
                <% end %>
            </ul>
        </form>
    </div>
  </nav>
</div>
